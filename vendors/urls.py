from django.contrib import admin
from django.urls import path
from . import views

urlpatterns = [
    # path('', views.vendor_home, name="home"),
    # path('signup', views.vendor_signup, name="signup"),
    # path('signin', views.vendor_signup, name="signin"),
]
    